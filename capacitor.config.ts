import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luciatello.scania',
  appName: 'Scania',
  webDir: 'dist/scania/browser'
};

export default config;
